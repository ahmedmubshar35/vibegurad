import 'package:flutter/material.dart'; class progress_ring extends StatelessWidget { const progress_ring({super.key}); @override Widget build(BuildContext context) { return const Scaffold(body: Center(child: Text('Coming Soon'))); } }
